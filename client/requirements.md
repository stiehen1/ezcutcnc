## Packages
prismjs | Syntax highlighting for code blocks
react-simple-code-editor | Lightweight code editor component
framer-motion | Smooth animations for UI interactions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
The app requires a dark mode aesthetic by default.
We will simulate Python execution on the frontend for the MVP using a mock runner or basic evaluation if feasible, but primarily focusing on the UI/UX of the editor.
